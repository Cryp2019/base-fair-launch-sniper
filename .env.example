# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
TELEGRAM_CHAT_ID=@your_channel_or_chat_id

# ===== RPC Configuration =====
# Set both keys for maximum reliability — the bot tries each as a fallback:
#
# Infura (free tier: 100k requests/day): https://developer.metamask.io/
INFURA_KEY=your_infura_project_id_here
#
# Alchemy (free tier available): https://dashboard.alchemy.com/
ALCHEMY_BASE_KEY=your_alchemy_api_key_here
#
# The bot uses BOTH when available, falling back through:
#   Infura → Alchemy → free public RPCs (publicnode, llamarpc, drpc, ankr, etc.)
#
# Optional: override with any custom RPC URL (takes top priority):
# BASE_RPC_URL=https://base-rpc.publicnode.com
